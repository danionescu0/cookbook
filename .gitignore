# env
.env

.claudes/

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.db
*.dump

# locally downloaded/processed recipe images (local IMAGES_DIR default, not the Docker volume)
api/data/
worker/data/

# local backup output (scripts/backup.sh) — DB dumps + images archives, never committed
backups/

# node
node_modules/
dist/
*.tsbuildinfo

# editors/os
.DS_Store
.idea/
